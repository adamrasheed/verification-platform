# GitHub check projector

Pure M7 presentation logic for GitHub Checks. It consumes a canonical command
envelope and emits a literal allowlist of status, reason codes, aggregate
counts, duration, Evidence classifications, and opaque run identity. It never
receives repository files and never emits annotations.

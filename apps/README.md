# Applications

Product interface applications belong here, including the CLI and future
GitHub, MCP, editor, agent, or cloud adapters.

Applications are transport and presentation layers over the canonical command
dispatcher. They MUST NOT reimplement discovery, evaluation, aggregation,
authorization, repair selection, or exit semantics.

The CLI is the canonical public interface and structured-output conformance
oracle.
